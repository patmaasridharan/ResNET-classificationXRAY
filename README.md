# Spine X-ray Binary Classification (ResNet18)

Train and evaluate a binary classifier for spine X-ray views (AP vs Lateral) using a ResNet18 feature extractor and a lightweight linear head.

## Project structure

notebooks folder contains the SVM(hog_classifier.ipynb) and ResNet18(resnet_classifier.ipynb) implementation
